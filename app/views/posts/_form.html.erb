<%= form_with model: @post, url: user_posts_path do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :text %>
  <%= form.text_area :text %>

  <%= form.submit "Submit" %>
<% end %>